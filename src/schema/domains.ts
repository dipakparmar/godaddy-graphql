// import { V1Api } from 'godaddy-domains-ts'
import { builder } from '../builder'

// const api = new V1Api()
// const domainsList = api.list() // returns a Promise<DomainSummary[]>

// builder.objectType('GoDaddyDomains', {
//   fields: (t) => ({
//     domains: t.field({
//       type: 'GoDaddyDomain',
//       list: true,
//       resolve: (parent, args, ctx) => {
//         // This is the line that is causing the error:
//         return domainsList
//       },
//     }),
//   }),
// })
